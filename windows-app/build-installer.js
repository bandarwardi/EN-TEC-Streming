const { createWindowsInstaller } = require('electron-winstaller'); async function build() { try { await createWindowsInstaller({ appDirectory: 'dist/streaming-tv-desktop-win32-x64', outputDirectory: 'dist/installer', authors: 'Streaming TV', exe: 'streaming-tv-desktop.exe', setupIcon: 'build/icon.ico', noMsi: true, version: '1.0.0' }); console.log('Done!'); } catch (e) { console.error(e); } } build();
