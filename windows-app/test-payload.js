const http = require('http'); const req = http.request('http://localhost:52535/api/cache/write', { method: 'POST', headers: { 'Content-Type': 'application/json' } }, res => { let b = ''; res.on('data', c => b += c); res.on('end', () => console.log('Status:', res.statusCode, 'Body:', b)); }); req.on('error', console.error); req.write(JSON.stringify({ key: 'test', data: 'x'.repeat(100) })); req.end();
